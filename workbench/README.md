# Workbench

The Workbench is a small Laravel application for manually exercising the
package end to end.

Run it with:

```bash
composer serve
```

The automated package tests remain under `tests/`.
